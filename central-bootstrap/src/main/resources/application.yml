server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: central-studio
    version: @{ project.version }@

logging:
  config: classpath:logback-local.xml

# 应用配置
studio:
  provider:
    code: central-studio
    key: central-studio-key
    fetchers:
      - log
      - saas
